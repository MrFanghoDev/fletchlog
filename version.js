/* Numéro de version affiché dans le footer -- issue #20. Pas de build
 * ni de dérivation automatique possible ici (contrairement à
 * setuptools_scm côté FletchScore/FletchTime, voir CLAUDE.md section
 * "Versions") : maintenu à la main, à mettre à jour à chaque tag posé.
 */
const FLETCHLOG_VERSION = "v0.3.1";
